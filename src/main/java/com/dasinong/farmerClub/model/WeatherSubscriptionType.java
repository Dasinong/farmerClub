package com.dasinong.farmerClub.model;

public enum WeatherSubscriptionType {
	// order matters here!!!

	// the subscription is added via normal flow
	NORMAL,

	// the subscription comes from the field added by farmer
	FIELD
}
